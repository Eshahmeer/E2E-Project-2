apiVersion: v1
kind: Service
metadata:
  name: cypress
  namespace: demoproject
spec:
  selector:
    app: cypress
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 32020
  type: NodePort
